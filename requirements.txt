# requirements.txt — Claims Pipeline Dependencies
# ==============================================
# Covers Phase 1 (ETL), Phase 2 (Storage), Phase 5 (Serving),
# Phase 6 (Streaming), and supporting infrastructure.

# --- Web Framework (Phase 5) ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# --- Cassandra (Phase 2 + 5 + 6) ---
cassandra-driver>=3.28.0

# --- Kafka (Phase 5 + 6) ---
kafka-python>=2.0.2

# --- Data Processing (Phase 1 ETL) ---
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0
pyspark>=3.5.0

# --- Model compilation (Phase 3/4) ---
scikit-learn>=1.3.0
m2cgen>=0.11.0
xgboost>=2.0.0

# --- Experiment Tracking ---
mlflow>=2.9.0

# --- Utilities ---
requests>=2.31.0
